{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A contact form submission from a potential client.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The full name of the sender." },
        "email": { "type": "string", "format": "email", "description": "The email address of the sender." },
        "phone": { "type": "string", "description": "The phone number of the sender." },
        "message": { "type": "string", "description": "The message sent by the sender." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the lead was created." },
        "language": { "type": "string", "description": "The language the user was using when они submitted." }
      },
      "required": ["name", "email", "phone", "message", "createdAt"]
    }
  },
  "firestore": {
    "leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of contact form submissions."
    }
  }
}
